
export const updateMatchId = (newMatchId: string) => {
    // invalidate the matchId store
    // matchId.update(() => '');
    //matchId.set(newMatchId.toString());
    ("match id updated to: " + newMatchId);
}